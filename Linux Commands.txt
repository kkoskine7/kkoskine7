pwd
mkdir dir
cd dir
cd..
ls

touche file1
touche file2
cat file1 file2
cat file2 

cp file2 file1
cp file1 file2
mv file1 file2
rm file1
mv file1

grep pattern files
find /dir/ -name name*
whereis command
locate file

chmod -R 775 folder1
chmod -R 600 folder1
chmod 775 file1
chown user:group file

uname -a
head -n1 /etc/issue
mount
whoami
man command
ip addr show
whois domain
dig domain
hostname -l
wget file

df -h See free and used space on mounted systems
fdisk -l Display disc partitions, sizes and types
du -ah -sh See disc usage du for all files

du -ah See du for all files and directory
du -sh See du for the current directory
mount device_path mount_point Mount a device
yum install package_name.rpm Install a package using the YUM yum
dnf install package_name.rpm Install a package using the DNF dnf
apt -get install package_name.rpm Install a package using the APT apt
rpm -i package_name.rpm Install local Package Package_name.rpm
rpm -e package_name.rpm Remove local file
